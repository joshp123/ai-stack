---

# Codex Guidance

## Defaults
- Trusted `~/code/...` repos run with approvals off + no sandbox; if unsure, check `~/.codex/config.toml` or `/status`.

## Process
- PR/commit rules live in `docs/agents/PROCESS.md`.
